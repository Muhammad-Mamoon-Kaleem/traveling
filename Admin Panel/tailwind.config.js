/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './index.html', // for Vite (if you have an index.html at the root)
    './src/**/*.{js,jsx,ts,tsx}', // for React components
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
